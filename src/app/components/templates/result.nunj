{% for people in data.results %}
    <div class="search__result--item">
        <h3 class="search__result--name">{{ people.name }}</h3>
        <p>{{ people.description }}</p>
    </div>
{% endfor %}
