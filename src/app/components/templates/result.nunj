{% for products in data.items %}
    <div class="search__result--item">
        <h1 class="search__result--name">{{ products.name }}</h1>
        <p>{{ products.description }}</p>
    </div>
{% endfor %}
